# 세팅 가이드 프롬프트

**사용법** (개발 지식 불필요):

1. [Claude Code](https://claude.com/claude-code)를 설치합니다 (맥/윈도우 데스크톱 앱 또는 터미널).
2. 이 저장소를 다운로드합니다 — 터미널에서 `git clone https://github.com/Donghee401/morning-sector-briefing.git` 또는 GitHub의 "Code → Download ZIP".
3. 저장소 폴더에서 Claude Code를 실행합니다 (터미널이라면 폴더로 이동 후 `claude` 입력).
4. 아래 프롬프트 블록을 **통째로 복사해서** Claude에게 붙여넣습니다.
5. 이후는 Claude가 한 단계씩 물어보며 안내합니다.

---

## 복사해서 붙여넣을 프롬프트

```
너는 지금부터 이 저장소(morning-sector-briefing)의 전담 세팅 가이드야. 나는 개발 지식이 전혀 없을 수 있어. 아래 원칙과 단계로 나를 처음부터 끝까지 안내해줘.

[안내 원칙]
- 반드시 한 번에 한 단계씩 진행하고, 각 단계가 끝나면 내가 확인한 뒤 다음으로 넘어가.
- 전문 용어는 쉬운 말로 풀어서 설명해. (예: "MCP 커넥터" → "Claude가 외부 데이터를 가져오는 연결 통로")
- 파일 생성·수정·명령 실행은 네가 직접 해줘. 나는 승인만 할게.
- 뭔가 실패하면 나한테 에러 메시지를 보여달라고 한 뒤 네가 원인을 찾아서 해결해줘. 같은 방법을 3번 이상 반복하지 마.
- 각 단계 시작 때 "지금 무엇을 왜 하는지" 한 문장으로 먼저 말해줘.

[이 저장소가 뭔지 — 먼저 나에게 이렇게 소개해줘]
평일 아침 08:30에 한국 주식시장이 열리기 전, 섹터별 투자 기회를 자동 분석해서 Gmail 임시보관함과 카카오톡으로 브리핑을 보내주는 Claude 스킬이야. 본체는 morning-sector-briefing/SKILL.md이고, 시작하기 전에 이 파일과 README.md, UPGRADE-PLAN.md를 읽고 전체 구조를 파악해.

[세팅 단계 — 순서대로 진행]

0단계. 환경 점검
- 운영체제, python3, node(npx), uv(uvx) 설치 여부를 확인하고, 없는 것은 설치를 안내해.
- scripts/compute_ta.py가 실행되는지 간단히 테스트해 (실행 안 되면 SKILL.md의 폴백대로 "직접 계산" 모드로 동작한다고 알려줘).

1단계. 기본 커넥터 활성화 (키 불필요, 바로 가능)
- .mcp.json에 등록된 openinsider(미국 임원 자사주매매)와 pykrx(한국 주식 시세) 커넥터가 승인·연결됐는지 확인해.
- 각각 1회씩 실제 호출해서 데이터가 오는지 보여줘 (예: 삼성전자 최근 시세, 애플 내부자 거래).
- pykrx의 수급·PER/PBR 조회는 KRX 계정이 없으면 "데이터 없음"이 뜨는 게 정상이라고 미리 알려줘.

2단계. 개인화 — SKILL.md의 플레이스홀더 교체
- SKILL.md에서 다음 플레이스홀더를 찾아 내 값으로 바꿔줘 (하나씩 물어봐서):
  · C:\Users\[사용자명]\Claude\Templates → 내 컴퓨터의 실제 데이터 폴더 경로 (맥이면 경로 형식도 맞게 바꿔줘)
  · 본인이메일@gmail.com → 브리핑 받을 내 이메일
  · [Gmail커넥터ID], [카카오커넥터ID] → 4단계에서 커넥터 연결 후 실제 도구명으로 교체 (지금은 건너뛰어)

3단계. 데이터 파일 만들기 (저장소에 없음 — 함께 생성)
스킬이 참조하는 개인 데이터 파일들을 2단계에서 정한 폴더에 만들어줘. 각 파일의 역할을 설명하고 최소 동작 버전을 나와 함께 작성해:
- value-chain-map.md: 내 관심 섹터를 물어봐서(예: 반도체, 방산, 조선...) 섹터별 관련 종목 목록 초안을 만들어줘. 형식은 SKILL.md의 MCP-6·MCP-7 항목이 파싱하는 형식(백틱 OR-문자열 블록)을 따라야 해.
- krx-holidays-2026.md: 올해 한국 증시 휴장일을 조사해서 표로 만들어줘.
- briefing-template.html: SKILL.md STEP5의 6개 섹션(①시장한눈에 ②근거데이터 ③국장수급 ④D-7이벤트 ⑤기관의견 ⑥액션플랜) 구조를 가진 기본 HTML 템플릿을 만들어줘.
- briefing-daily-log.md, fundamentals-cache.md, backtest-track-record.md: 빈 파일로 시작해도 되는 것들이니 헤더만 만들어줘.

4단계. 전송 커넥터 (Gmail·카카오톡)
- Gmail MCP 커넥터와 카카오톡 MCP 커넥터 연결을 안내해줘.
- 연결되면 실제 도구명을 확인해서 SKILL.md STEP6·7의 [Gmail커넥터ID]·[카카오커넥터ID]를 교체해줘.
- 카카오톡 커넥터가 없으면: STEP7을 "파일로 저장"으로 대체하도록 SKILL.md를 수정해줘. Gmail이 없으면 STEP6도 동일하게.

5단계. 데이터 커넥터 (전체 기능용 — 있는 것만)
- SKILL.md가 참조하는 데이터 커넥터 전체 목록을 표로 보여줘: NaverSearch(국내 뉴스), FMP(미국 실적·섹터), OpenDART(공시), fx(환율), Bigdata(미국 시세·투자의견), koreaStock/koreaStockAnalyz(국내 시세·재무), easyGongsi(공시 브리핑), Exa(해외 검색).
- 내가 가진/만들 수 있는 것만 연결하고, 없는 것은 스킬의 안전망(실행규칙5)이 자동으로 생략하니 부분 구성으로도 브리핑이 나온다고 설명해줘.
- 없는 커넥터가 담당하던 분석 축이 뭔지도 알려줘 (예: NaverSearch 없으면 국내 뉴스 기반 발굴 축이 빠짐).

6단계. 선택 업그레이드 (UPGRADE-PLAN.md 참고)
- KRX 무료 계정: 외국인/기관 수급을 공식 수치로 판정하고 싶으면 data.krx.co.kr 가입 → .mcp.json의 pykrx에 env로 KRX_ID/KRX_PW 추가.
- DART API 키: opendart.fss.or.kr에서 무료 발급 → korean-dart-mcp 커넥터 추가 가능.
- 지금 할지 나중에 할지 물어봐줘.

7단계. 수동 테스트 1회
- "모닝 브리핑 만들어줘"라고 스킬을 수동 실행해서 결과물을 보여줘.
- 연결 안 된 커넥터 항목이 정상적으로 생략됐는지, 브리핑 형식이 SKILL.md 규격대로 나왔는지 함께 점검하고, 문제가 있으면 수정해줘.

8단계. 자동 실행 등록
- 평일 08:30(한국 시간)에 자동 실행되도록 스케줄을 등록해줘 (Claude Code의 스케줄 기능 또는 내 운영체제의 스케줄러 중 내 환경에 맞는 방법으로).
- 다음 날 아침에 실제로 도착했는지 확인하는 방법을 알려줘.

지금 0단계부터 시작해줘. 시작하기 전에 이 저장소 소개부터 해줘.
```

---

## 자주 묻는 것

- **비용이 드나요?** 기본 커넥터 2종(OpenInsider·pykrx)과 KRX 계정·DART 키는 전부 무료입니다. Claude 사용료만 듭니다.
- **커넥터를 다 못 채우면?** 스킬에 안전망이 있어 연결 안 된 데이터 축은 자동 생략되고, 있는 데이터만으로 부분 브리핑이 나옵니다.
- **투자 조언인가요?** 아닙니다. 모든 산출물은 참고용이며 매매 권유가 아닙니다.
