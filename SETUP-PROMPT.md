# 세팅 가이드 프롬프트

**사용법** (개발 지식 불필요 — 클론·설치·연결 전부 Claude가 해줍니다):

1. [Claude Code](https://claude.com/claude-code)를 설치합니다 (맥/윈도우 데스크톱 앱 또는 터미널).
2. Claude Code를 실행합니다 — 아무 폴더에서나 괜찮습니다.
3. 이 페이지의 아래 프롬프트 블록을 **통째로 복사해서** Claude에게 붙여넣습니다.
4. 이후는 Claude가 저장소 다운로드부터 자동 실행 등록까지 한 단계씩 안내합니다.
5. 중간에 Claude가 "재시작 후 이 프롬프트를 다시 붙여넣어 달라"고 하면 그대로 하면 됩니다 — 완료된 단계는 건너뛰고 이어서 진행되도록 설계돼 있습니다.

---

## 복사해서 붙여넣을 프롬프트

```
너는 지금부터 morning-sector-briefing 저장소(https://github.com/Donghee401/morning-sector-briefing)의 전담 세팅 가이드야. 나는 개발 지식이 전혀 없을 수 있어. 아래 원칙과 단계로 나를 처음부터 끝까지 안내해줘.

[안내 원칙]
- 반드시 한 번에 한 단계씩 진행하고, 각 단계가 끝나면 내가 확인한 뒤 다음으로 넘어가.
- 이 프롬프트는 중간에 Claude를 재시작한 뒤 다시 붙여넣어도 이어서 진행되도록, 각 단계를 시작하기 전에 이미 완료된 상태인지 먼저 확인하고 완료된 단계는 결과만 한 줄 보고하고 건너뛰어.
- 전문 용어는 쉬운 말로 풀어서 설명해. (예: "MCP 커넥터" → "Claude가 외부 데이터를 가져오는 연결 통로")
- 파일 생성·수정·명령 실행·커넥터 등록은 전부 네가 직접 해. 나는 승인만 할게. 단, 뭔가를 설치하거나 시스템을 바꾸는 명령은 실행 전에 "지금 무엇을 왜 하는지" 한 문장으로 먼저 말해줘.
- 뭔가 실패하면 에러 내용을 네가 직접 읽고 원인을 찾아 해결해. 같은 방법을 3번 이상 반복하지 말고, 막히면 대안을 제시해.

[이 저장소가 뭔지 — 먼저 나에게 이렇게 소개해줘]
평일 아침 08:30에 한국 주식시장이 열리기 전, 섹터별 투자 기회를 자동 분석해서 Gmail 임시보관함과 카카오톡으로 브리핑을 보내주는 Claude 스킬이야. 본체는 morning-sector-briefing/SKILL.md이고, 저장소를 받은 뒤 이 파일과 README.md, UPGRADE-PLAN.md를 읽고 전체 구조를 파악해.

[세팅 단계 — 순서대로, 완료된 단계는 건너뛰며 진행]

0단계. 저장소 받기 (네가 직접)
- git이 설치돼 있는지 확인하고, 없으면 네가 설치를 진행해.
- 나에게 저장소를 어디에 둘지 물어봐 (기본 제안: 홈 폴더 아래 morning-sector-briefing).
- git clone https://github.com/Donghee401/morning-sector-briefing.git 을 네가 실행해. 이미 클론돼 있으면 git pull로 최신화만 해.
- 현재 Claude 세션이 저장소 폴더 밖에서 실행 중이라면 기억해뒀다가 2단계에서 재시작 안내에 사용해.

1단계. 실행 환경 자동 준비
- python3, node(npx), uv(uvx) 설치 여부를 확인하고, 없는 것은 네가 직접 설치를 진행해 (맥: Homebrew 또는 공식 설치 스크립트, 윈도우: winget). 각 설치 전에 한 문장 설명.
- 설치 후 scripts/compute_ta.py가 실행되는지 테스트해 (실행 불가 환경이면 SKILL.md의 폴백대로 "직접 계산" 모드로 동작한다고 알려줘).

2단계. 기본 커넥터 자동 등록·연결 (키 불필요)
- 네가 직접 아래 두 커넥터를 등록해:
  claude mcp add --scope user openinsider -- npx -y openinsider-mcp
  claude mcp add --scope user pykrx -- uvx --with "mcp<2" pykrx-mcp
  (저장소 폴더에서 시작한 세션이면 .mcp.json이 이미 인식됐을 수 있으니 그 경우 승인 여부만 확인해.)
- 등록 후 현재 세션에서 이 도구들이 보이지 않으면: 나에게 "저장소 폴더에서 Claude를 재시작한 뒤 이 프롬프트를 다시 붙여넣어 달라"고 안내하고 이번 턴을 끝내.
- 도구가 보이면 각각 1회 실호출로 검증해 (예: 삼성전자 최근 시세, 애플 내부자 거래). pykrx의 수급·PER/PBR 조회는 KRX 계정이 없으면 "데이터 없음"이 뜨는 게 정상이라고 미리 알려줘.

3단계. 개인화 — SKILL.md 플레이스홀더 교체
- SKILL.md에서 다음을 찾아 내 값으로 바꿔줘 (하나씩 물어봐서):
  · C:\Users\[사용자명]\Claude\Templates → 내 컴퓨터의 실제 데이터 폴더 경로 (내 운영체제에 맞는 형식으로, 폴더가 없으면 네가 생성)
  · 본인이메일@gmail.com → 브리핑 받을 내 이메일
  · [Gmail커넥터ID], [카카오커넥터ID] → 5단계에서 커넥터 연결 후 실제 도구명으로 네가 교체 (지금은 건너뛰어)

4단계. 데이터 파일 만들기 (저장소에 없음 — 함께 생성)
스킬이 참조하는 개인 데이터 파일들을 3단계에서 정한 폴더에 네가 만들어줘. 각 파일의 역할을 설명하고 최소 동작 버전을 나와 함께 작성해:
- value-chain-map.md: 내 관심 섹터를 물어봐서(예: 반도체, 방산, 조선...) 섹터별 관련 종목 목록 초안을 만들어줘. 형식은 SKILL.md의 MCP-6·MCP-7 항목이 파싱하는 형식(백틱 OR-문자열 블록)을 따라야 해.
- krx-holidays-2026.md: 올해 한국 증시 휴장일을 조사해서 표로 만들어줘.
- briefing-template.html: SKILL.md STEP5의 6개 섹션(①시장한눈에 ②근거데이터 ③국장수급 ④D-7이벤트 ⑤기관의견 ⑥액션플랜) 구조의 기본 HTML 템플릿을 만들어줘.
- briefing-daily-log.md, fundamentals-cache.md, backtest-track-record.md: 헤더만 있는 빈 파일로 생성.

5단계. 전송 커넥터 (Gmail·카카오톡) — 가능한 한 자동
- 현재 연결된 MCP 목록에서 Gmail·카카오톡 커넥터가 이미 있는지 먼저 확인해.
- 없으면 설치 가능한 MCP를 조사해서 네가 등록까지 진행하고, OAuth 로그인·API 키처럼 반드시 사람이 해야 하는 인증 단계만 나에게 화면 기준으로 안내해.
- 연결되면 실제 도구명을 확인해 SKILL.md STEP6·7의 [Gmail커넥터ID]·[카카오커넥터ID]를 네가 교체해.
- 끝내 연결이 어려우면: STEP6/7을 "브리핑을 파일로 저장"으로 대체하도록 SKILL.md를 네가 수정하고, 나중에 커넥터가 생기면 되돌리는 방법을 한 줄 남겨줘.

6단계. 데이터 커넥터 (전체 기능용) — 있는 것부터, 자동으로
- SKILL.md가 참조하는 데이터 커넥터 전체 목록을 표로 보여줘: NaverSearch(국내 뉴스), FMP(미국 실적·섹터), OpenDART(공시), fx(환율), Bigdata(미국 시세·투자의견), koreaStock/koreaStockAnalyz(국내 시세·재무), easyGongsi(공시 브리핑), Exa(해외 검색). 각각이 담당하는 분석 축도 함께.
- 각 항목에 대해: 공개 MCP가 있으면 네가 설치·등록까지 진행하고, API 키가 필요한 것은 발급 페이지와 절차를 안내한 뒤 내가 키를 주면 네가 등록을 마무리해.
- 연결 못 한 것은 스킬의 안전망(실행규칙5)이 자동 생략하니 부분 구성으로도 브리핑이 나온다고 설명해줘.

7단계. 선택 업그레이드 (UPGRADE-PLAN.md 참고)
- KRX 무료 계정: 외국인/기관 수급을 공식 수치로 판정하려면 data.krx.co.kr 가입 → 내가 ID/PW를 주면 네가 pykrx 커넥터 env(KRX_ID/KRX_PW)에 등록.
- DART API 키: opendart.fss.or.kr 무료 발급 → 내가 키를 주면 네가 korean-dart-mcp(env DART_API_KEY)를 등록.
- 지금 할지 나중에 할지 물어봐줘.

8단계. 수동 테스트 1회
- "모닝 브리핑 만들어줘"로 스킬을 수동 실행해 결과물을 보여줘.
- 연결 안 된 커넥터 항목이 정상적으로 생략됐는지, 브리핑 형식이 SKILL.md 규격대로 나왔는지 함께 점검하고 문제가 있으면 수정해.

9단계. 자동 실행 등록
- 평일 08:30(한국 시간) 자동 실행 스케줄을 네가 등록해줘 (Claude Code의 스케줄 기능 또는 내 운영체제의 스케줄러 중 내 환경에 맞는 방법으로).
- 다음 날 아침에 실제로 도착했는지 확인하는 방법을 알려줘.

지금 0단계부터 시작해줘. 시작하기 전에 이 저장소 소개부터 해줘.
```

---

## 자주 묻는 것

- **비용이 드나요?** 기본 커넥터 2종(OpenInsider·pykrx)과 KRX 계정·DART 키는 전부 무료입니다. Claude 사용료만 듭니다.
- **중간에 재시작하라고 하면?** 시키는 대로 재시작한 뒤 같은 프롬프트를 다시 붙여넣으면 됩니다. 완료된 단계는 자동으로 건너뜁니다.
- **커넥터를 다 못 채우면?** 스킬에 안전망이 있어 연결 안 된 데이터 축은 자동 생략되고, 있는 데이터만으로 부분 브리핑이 나옵니다.
- **투자 조언인가요?** 아닙니다. 모든 산출물은 참고용이며 매매 권유가 아닙니다.
